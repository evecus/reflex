name: Test

on:
  workflow_dispatch:


env:
  CARGO_TERM_COLOR: always
  FEATURES: outbound-net

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-test-

      - name: Update Cargo.lock
        run: cargo update -p tokio-tungstenite

      - name: Commit updated Cargo.lock
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Cargo.lock
          git diff --cached --quiet || git commit -m "chore: update Cargo.lock [skip ci]"
          git push

      - name: Run tests
        run: cargo test --locked

      - name: Run clippy
        run: cargo clippy --locked -- -D warnings

  # ── 3. 交叉编译 reflex ────────────────────────────────────────────────────
  build:
    name: Build / ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    needs: test
    strategy:
      fail-fast: false
      matrix:
        include:
          - target:   aarch64-unknown-linux-musl
            os:       ubuntu-latest
            artifact: reflex-linux-aarch64
            cross:    true

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-build-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.target }}-cargo-build-
            ${{ runner.os }}-${{ matrix.target }}-cargo-

      - name: Install musl-tools (x86_64)
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: sudo apt-get install -y musl-tools

      - name: Install cross (aarch64)
        if: matrix.cross == true
        uses: taiki-e/install-action@v2
        with:
          tool: cross

      # ── 只编译 reflex（内置 rsc 功能） ─────────────────────────────────
      - name: Build reflex (x86_64, native)
        if: matrix.cross == false
        run: |
          cargo build \
            --release \
            --locked \
            --target ${{ matrix.target }} \
            --bin reflex \
            --features outbound-net
        env:
          CC_x86_64_unknown_linux_musl: musl-gcc

      - name: Build reflex (aarch64, cross)
        if: matrix.cross == true
        run: |
          cross build \
            --release \
            --locked \
            --target ${{ matrix.target }} \
            --bin reflex \
            --features outbound-net

      # ── 收集产物 ────────────────────────────────────────────────────────
      - name: Collect binary
        run: |
          mkdir -p dist
          BIN_DIR="target/${{ matrix.target }}/release"
          cp "$BIN_DIR/reflex" "dist/reflex-linux-aarch64"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name:   test
          name:       "reflex test"
          prerelease: true
          draft:      false
          files:      dist/reflex-linux-aarch64
